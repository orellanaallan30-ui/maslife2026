node_modules/
.env
.env.local
.env.*.local
dist/
dist-ssr/
.DS_Store
*.log
.vercel

# Lienzos de Claude Design: se versionan las fuentes editables (*.dc.html y
# canvas.json), no el archivo publicado — son ~2 MB de editor empaquetado que
# se regenera desde esas fuentes.
design/*.html
!design/*.dc.html

# Paquete descargable y copia de trabajo extraída del lienzo publicado:
# ambos se regeneran desde design/*.dc.html
landing-agenda-life.zip
design/actual/

# Recursos de la detección de puntos anatómicos: ~18 MB que se preparan
# en cada compilación (maslife2026/scripts/preparar-biomecanica.mjs).
maslife2026/public/mediapipe/
